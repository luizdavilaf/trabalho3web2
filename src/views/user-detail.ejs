

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- user.name %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css">
    
    </head>
<body>
    <div class="box">
        <%- include('./includes/header') %>
        <div class="container">  
           <%- include('./includes/navbar2', { user: user || undefined}) %>
        
            <main>
                <div class="mainContainer">
                    <h1>Posts de <%- user.name %></h1>

                    <ul class="post-list">
                    
                        <table class="table table-striped table-hover table-bordered table-dark ">
                            <thead>
                                <tr>
                                    <th>Titulo</th>
                                    <th><a href="/users/<%-user.name%>">Data de Publicação</a></th>
                                    <th><a href="/users/<%-user.name%>?order=likes">Contagem de likes</a></th>
                                                                
                                </tr>
                            <thead>
                            <tbody id="postsDoUsuario">

                                <% if(posts.length){ %>                    
                                    <% for(let i = 0; i < posts.length; i++) { %>
                                            <tr>
                                                
                                                <td><%=posts[i].title%></td>
                                                <td><%= new Date(posts[i].createdAt).toLocaleDateString();%></td>
                                                <td><%=posts[i].likeCount%></td>
                                                <td><a href="/posts/<%-posts[i].id%>">Ir para o Post</a> </td>        
                                                                        
                                            </tr>
                                        <% } %>
                                
                                <% }else{ %>
                                    <tr>
                                        <td>Sem posts para o usuário</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>            
                    </ul>
                </div>
            </main>
        </div>
    </div>
</body>

</html>